Inv.Database = {
    CreateInventory = {},
    HasInventory = {},
}

function Inv.Database.CreateInventory(name, meta, data) 
    local m = Mesh.Database
end

function Inv.Database.HasInventory(name) 
    
end